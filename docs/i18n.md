# i18n

The internationalization system in this project is built around a single source of truth for both copy and behavior. Source translations live as JSON under `src/locales`, while the full i18n runtime (locale data, formatters, i18next init, Alpine store, Nunjucks template) lives in `src/scripts/lib/i18n/`. The same keys are consumed by the Nunjucks template helpers for server-side rendering and by an i18next runtime that updates the DOM in place when the user switches language.

This document is the canonical reference for the system. It is accurate as of the file's "Last updated" line at the bottom and tracks the actual implementation, not aspirational behavior.

## Table of Contents

- [Architecture at a Glance](#architecture-at-a-glance)
- [Source Translations](#source-translations)
- [Template API](#template-api)
- [Runtime](#runtime)
- [Locale Configuration](#locale-configuration)
- [Translation Coverage](#translation-coverage)
- [Type Safety](#type-safety)
- [Tooling and Workflows](#tooling-and-workflows)
- [Best Practices](#best-practices)
- [Troubleshooting](#troubleshooting)
- [Reference](#reference)

## Architecture at a Glance

### File map

| Area | File | Purpose |
| --- | --- | --- |
| i18n user config | `src/configs/i18n.ts` | `i18nConfig` — `defaultLocale` + `fonts` (the only config user touches) |
| Path constants | `src/configs/paths.ts` | `PATHS` (filesystem paths) |
| Site config | `src/configs/site.ts` | `ROOT_PAGE` and site-level settings |
| Locale data (master) | `src/scripts/lib/i18n/data.ts` | `LOCALES` array — 87 locale entries |
| Locale data (per-concern) | `src/scripts/lib/i18n/*.ts` | `languages.ts`, `regions.ts`, `currencies.ts`, `timezones.ts`, `numbering-systems.ts`, `writing-systems.ts`, `flags.ts`, `directions.ts`, `calendars.ts` |
| Common types | `src/scripts/lib/i18n/types.ts` | `I18nConfig`, `FontConfig`, `FontStack`, `I18nItem`, `RegionalPrice`, `FormatOptions`, etc. |
| i18n builders | `src/scripts/lib/i18n/i18n-config.ts` | `defineI18n`, `defineFont` — type-safe builders with runtime validation |
| Engine helpers | `src/scripts/lib/i18n/helpers.ts` | `getLocale`, `getCurrency`, `getTimezone`, `getFallbackChain`, `getPluralSuffix`, etc. |
| Formatters | `src/scripts/lib/i18n/formatters.ts` | `Intl`-backed numbers, dates, currency, prices, plurals, digits |
| Microtask queue | `src/scripts/lib/utils/microtask-queue.ts` | `scheduleTask()` — batched DOM updates via microtask queue |
| Barrel exports | `src/scripts/lib/i18n/index.ts` | Re-exports data + helpers + formatters (no i18next/DOM) |
| JSON5 reader | `src/scripts/utils/json5.ts` | `readJSON5`, `loadGlobalData`, `loadSelectedComponentLocales` — reads `components.${name}.json` (flat file) |
| Font engine | `src/scripts/lib/i18n/fonts.ts` | Font loading, `setupFontStackCSS`, `watchScriptAndLoadFont` |
| Template helpers | `src/scripts/lib/i18n/template.ts` | Nunjucks `i18n.*` API and inline client bootstrap |
| Runtime i18n | `src/scripts/lib/i18n/runtime.ts` | i18next init, `translatePage`, `updateFormattedElements` (inline `FORMATTERS` array), `t()` |
| Alpine store | `src/scripts/lib/i18n/store.ts` | `$store.i18n.languages`, `current`, `change()` |
| App bootstrap | `src/scripts/main.ts` | Vendor loading, font setup, store registration, `initIntl()` |
| Window globals | `src/global.d.ts` | `window.__PAGE_ID__`, `__USED_COMPONENTS__`, `__SAVED_LOCALE__`, `__SERVER_LOCALE__` |
| Source translations | `src/locales/{locale}/**/*.json` | JSON files edited by developers |
| Generated types | `generated/i18n.d.ts` | `I18nTranslationKeys`, `I18nPages`, `I18nComponents` — gitignored; stub committed for fresh-clone typecheck |
| Exchange rates | `generated/exchange-rates.ts` | `EXCHANGE_RATES`, `convertCurrency()` — gitignored; stub committed for fresh-clone typecheck |
| Type generator | `tools/generate-i18n.ts` | Generates `generated/i18n.d.ts`, errors on locale parity issues (fails build), syncs `i18n-ally.sourceLanguage`/`displayLanguage` from `i18nConfig.defaultLocale` |
| Sitemap generator | `tools/generate-sitemap.ts` | Generates `public/sitemap.xml` from page directories |
| Manifest generator | `tools/generate-manifest.ts` | Generates `public/manifest.json` from `global.json5` + `i18nConfig` — `start_url` and `scope` include `BASE_PATH` |
| Robots generator | `tools/generate-robots.ts` | Generates `public/robots.txt` from `SITE_URL` |
| Root page sync | `tools/sync-root-page.ts` | Ensures root page folder name matches `ROOT_PAGE` config |
| Locale sync | `tools/sync-locales.ts` | Creates missing locale folders from the default |
| Parity check | `tools/check-locale-parity.ts` | Diff keys across all locales |
| Rate fetcher | `tools/fetch-exchange-rates.ts` | Pulls rates from Frankfurter (base: `USD`), writes `exchange-rates.ts` |
| Watcher | `tools/watch-i18n.ts` | Logs locale changes (no regeneration) |
| Page generator | `tools/generate-page.ts` | Scaffolds a new page folder |
| Tool logger | `tools/shared/logger.ts` | Centralized `log.*()` and `logBox()` for formatted box output |
| Signal handler | `tools/shared/signal-handler.ts` | SIGINT handling, `wrapMainError()`, `handleExitPromptError()`, `createServer()` |
| Hono server | `tools/shared/hono-server.ts` | `createStaticApp()`, `loadHtmlCache()`, `getPageNames()` — shared static server |
| Site URL | `tools/shared/site-url.ts` | `SITE_URL` constant from `process.env` |
| Write file | `tools/shared/write-file.ts` | `writeFilePath()` (mkdir + write) and `generatedHeader()` for auto-generated files |

### Data flow

```txt
┌─────────────────────────────────────────────────────────────────────┐
│                      BUILD TIME (Nunjucks)                          │
│                                                                     │
│  src/locales/{locale}/*.json                                       │
│          │                                                          │
│          └─► template.ts (createTemplateParams)                    │
│                ├─► Reads JSON for the default locale               │
│                ├─► Resolves keys at render time                     │
│                └─► Emits HTML with data-* attributes                │
│                                                                     │
│  src/scripts/lib/i18n/index.ts (barrel: data + helpers + formatters)  │
│          │                                                          │
│          └─► Generates inline bootstrap script                      │
│                ├─► window.__PAGE_ID__     (current page id)         │
│                ├─► window.__SAVED_LOCALE__                          │
│                ├─► window.__SERVER_LOCALE__ (default locale)        │
│                └─► <html lang> + data-script + dir + .is-rtl        │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     RUNTIME (Browser)                               │
│                                                                     │
│  1. Inline script (synchronous, before page paint)                 │
│     └─► Sets <html lang>, data-script, dir                         │
│                                                                     │
│  2. main.ts bootstrap                                               │
│     ├─► registerI18nStore() + Alpine.start()                        │
│     ├─► initIntl(locale) (lib/i18n/runtime.ts)                      │
│     │    ├─► Fetch ${BASE_PATH}assets/i18n/{pageID}/{locale}.json       │
│     │    ├─► setLocale() from localStorage                          │
│     │    ├─► i18next.init() with resources                          │
│     │    └─► scheduleTask(translatePage) + scheduleTask(updateFormattedElements)  │
│     ├─► preloadActiveFont() (lib/i18n/fonts.ts)                      │
│     │    └─► dynamic import() of locale font CSS                     │
│     ├─► requestIdleCallback: setupFontStackCSS() + loadLanguageFonts() + watchScriptAndLoadFont() │
│     │    ├─► setupFontStackCSS()  (--font-primary from writing system) │
│     │    ├─► loadLanguageFonts()  (dynamic import)                  │
│     │    └─► watchScriptAndLoadFont() (MutationObserver: react to <html lang> changes) │
│                                                                     │
│  3. User changes language (Alpine store $store.i18n.change)         │
│     ├─► localStorage.setItem                                       │
│     ├─► ensureLocaleData() — fetch bundle if not loaded            │
│     ├─► i18next.changeLanguage()                                    │
│     ├─► scheduleTask(translatePage + updateFormattedElements)       │
│     ├─► updateDocumentAttributes() — <html lang>, dir, data-script │
│     └─► refreshFonts() → setupFontStackCSS + loadLanguageFonts     │
└─────────────────────────────────────────────────────────────────────┘
```

### Locales

The configured list currently contains **87 BCP 47 locale codes** spanning 54 languages. The default locale is `id-ID`.

#### Bundled locales (42, original set)

`id-ID`, `en-US`, `en-GB`, `en-CA`, `en-AU`, `en-IN`, `en-NZ`, `en-ZA`, `ja-JP`, `zh-Hans-CN`, `zh-Hans-SG`, `zh-Hans-MY`, `zh-Hant-TW`, `zh-Hant-HK`, `zh-Hant-MO`, `ar-SA`, `ar-AE`, `ar-EG`, `ar-MA`, `ar-TN`, `es-ES`, `es-MX`, `es-AR`, `es-CO`, `es-PE`, `pt-BR`, `pt-PT`, `pt-AO`, `pt-MZ`, `hi-IN`, `hi-NP`, `ko-KR`, `ko-KP`, `fr-FR`, `fr-CA`, `fr-BE`, `fr-CH`, `de-DE`, `de-AT`, `de-CH`, `ru-RU`, `th-TH`.

#### European Latin locales (12, added June 2026)

`nl-NL`, `ca-ES`, `it-IT`, `no-NO`, `fi-FI`, `sv-SE`, `da-DK`, `cs-CZ`, `hu-HU`, `ro-RO`, `pl-PL`, `el-GR`.

#### Turkic locales (3)

`tr-TR`, `az-AZ`, `uz-UZ`.

#### Central Asian Cyrillic locales (5)

`kk-KZ`, `ky-KG`, `tg-TJ`, `mn-MN`, `uz-AF`.

#### South Asian Indic locales (11)

`bn-BD`, `bn-IN`, `gu-IN`, `mr-IN`, `pa-IN`, `pa-PK`, `ta-IN`, `ta-LK`, `te-IN`, `kn-IN`, `ml-IN`.

#### Southeast Asian locales (1)

`vi-VN`.

#### Middle Eastern / Caucasus locales (4)

`he-IL`, `fa-IR`, `hy-AM`, `ka-GE`.

#### Slavic / Baltic / Balkan locales (6)

`uk-UA`, `sr-RS`, `hr-HR`, `sl-SI`, `sk-SK`, `bg-BG`.

#### Baltic locales (3)

`lt-LT`, `lv-LV`, `ee-EE`.

All 87 locales are translated — see [Translation Coverage](#translation-coverage) for status and locale-specific notes.

## Source Translations

### File layout

```txt
src/locales/
  id-ID/                       # default locale
    common.json               # site-wide copy
    home.json                 # page-specific copy
    pricing.json
    components.cta.json       # reusable component copy (flat, dot-notation namespace)
  en-US/
    common.json
    home.json
    ...
```

Locale files use `.json`. Data files (`src/data/`, `src/pages/*/index.json5`) use `.json5`. The `readJSON5` utility parses `.json` files as well, but the template and runtime paths always reference `.json` — there is no automatic `.json5` fallback for locale files.

### Namespace resolution

The template key prefix determines the i18next namespace at runtime.

| Template key | Runtime namespace | Source file |
| --- | --- | --- |
| `site_name` | `common:site_name` | `src/locales/{locale}/common.json` |
| `home:hero.title` | `home:hero.title` | `src/locales/{locale}/home.json` |
| `components.cta:heading` | `components.cta:heading` | `src/locales/{locale}/components.cta.json` |

The `i18next` default namespace is `common`. The page namespace is registered on `i18next.init` along with any namespaces for components used by the current page (the component list is detected by `getUsedComponents()` in `template.ts`).

### Plural keys

i18next pluralization is keyed by suffix. The suffix for a given count is computed by `getPluralSuffix(count, locale?)` using the browser's `Intl.PluralRules` (no per-locale config required — categories are inferred from the active locale automatically):

| `Intl.PluralRules` category | i18next key suffix |
| --- | --- |
| `zero` | `_zero` |
| `one` | `_one` |
| `two` | `_two` |
| `few` | `_few` |
| `many` | `_many` |
| `other` | `_other` |

```json5
// src/locales/id-ID/home.json
{
  hero: {
    title: "Build multilingual pages faster",
    subtitle: "One translation source for server rendering and runtime switching.",
  },
  stats: {
    projects_one: "{{count}} proyek",
    projects_other: "{{count}} proyek",
    projects_zero: "0 proyek",
    projects_two: "{{count}} proyek",
    projects_few: "{{count}} proyek",
    projects_many: "{{count}} proyek",
  },
}
```

```njk
{{ i18n.t('home:hero.title') }}
{{ i18n.plural('home:stats.projects', 7) }}
```

### Variables

Variables use the `{{name}}` syntax and are interpolated by both the SSR `resolve()` and i18next at runtime.

```json5
{ welcome: "Hello {{name}}" }
```

```njk
{{ i18n.t('welcome', { name: 'Dinda' }) }}
```

## Template API

The Nunjucks `i18n` object is created in `src/scripts/lib/i18n/template.ts` and is available to every template and partial that extends `main.njk`.

### Translation API

| API | Returns | Notes |
| --- | --- | --- |
| `i18n.t(key, vars?, options?)` | Wrapped HTML | `data-i18n` span; refreshes on language change. Pass `{ raw: true }` for plain string |
| `i18n.html(key, vars?)` | Raw locale string | Returns the resolved string with no wrapper (useful inside attribute values) |
| `i18n.attr(key, attrName, vars?)` | `attrName="…"` | Emits a complete attribute, e.g. `alt="…"` |
| `i18n.plural(key, count, vars?, options?)` | Wrapped HTML | `data-i18n-plural` + `data-i18n-count`; refreshes on language change |
| `i18n.getPluralSuffix(count)` | `_one` \| `_other` \| `_few` \| `_many` \| `_zero` \| `_two` | Useful for direct key construction |

`i18n.t()` options:

| Option | Effect |
| --- | --- |
| `raw: true` | Return a string without the wrapper span |
| `native: true` | Force native digits in the output (`data-force-native`) |
| `universal: true` | Force Latin digits (`0-9`) in the output (`data-force-universal`) |
| `className: '…'` | Add a class to the wrapper span |

`i18n.plural()` options: `raw`, `className`. The `count` is automatically added to the `vars` and the correct suffix is appended to the key for SSR.

Example output:

```html
<span data-i18n="home:hero.title">Build multilingual pages faster</span>
```

### Formatting API

Every formatter renders a `<span>` with one or more `data-format-*` attributes. `updateFormattedElements()` walks the DOM and replaces the inner text whenever the active locale changes. Pass `{ raw: true }` to receive a plain string.

| API | Data attribute | Notes |
| --- | --- | --- |
| `formatNumber(value, options?)` | `data-format-number` | `Intl.NumberFormat` |
| `formatCurrency(value, currency, options?)` | `data-format-currency` + `data-target-currency` | Currency code is required |
| `formatPercent(value, options?)` | `data-format-percent` | `0.85` → `85%` |
| `formatBytes(bytes, decimals = 1, options?)` | `data-format-bytes` + `data-bytes-decimals` | Uses binary units (`KiB`-style but labeled `KB`) |
| `formatDuration(seconds, options?)` | `data-format-duration` | Combines `Intl.RelativeTimeFormat` and `Intl.ListFormat` |
| `formatDate(date, options?)` | `data-format-date` | Uses locale timezone and calendar |
| `formatTime(date, preset?, options?)` | `data-format-time` + `data-time-preset` | `preset` is `'short'` \| `'medium'` \| `'long'` \| `'full'` |
| `formatDateTime(date, options?)` | `data-format-datetime` | Combines date and time |
| `formatOrdinal(value, options?)` | `data-format-ordinal` | Manual strategies for `id`, `ja`, `ar`; `Intl.PluralRules` + English suffix map for all other locales |
| `formatCardinal(value, options?)` | `data-format-cardinal` | Manual strategies for `id`, `ja`, `zh`, `ar`; otherwise returns the raw number string (`Intl` has no API for cardinal spellout). `options.gender: 'masculine' \| 'feminine'` only affects the Arabic strategy. |
| `formatScientific(value, options?)` | `data-format-scientific` | `Intl` scientific notation with native digit fallbacks |
| `formatAbbreviated(value, options?)` | `data-format-abbreviated` | Compact form such as `15.3K` |
| `formatList(items, options?)` | `data-format-list` + `data-items` | `Intl.ListFormat` |
| `formatUnit(value, unit, options?)` | `data-format-unit` + `data-unit` | `Intl.NumberFormat` with `style: 'unit'` |
| `formatRelativeTime(value, options)` | `data-relative-time` + `data-unit` + `data-numeric` | `options.unit` is required; `options.numeric` is `'always'` \| `'auto'` (default `'always'`) |
| `nativeDigits(text, force?, options?)` | `data-native-digits` + `data-force` | Initial conversion + refreshed at runtime |
| `pluralize(word, count?, inclusive?, options?)` | `data-pluralized` + `data-count` + `data-inclusive` | Initial English pluralization; refreshed at runtime |
| `singularize(word, options?)` | `data-singularized` | Initial English singularization; refreshed at runtime |
| `getPluralSuffix(count)` | – | Returns the suffix string |

Common options supported across the numeric formatters:

| Option | Effect |
| --- | --- |
| `raw: true` | Return a string without a wrapper span |
| `className: '…'` | Add a class to the wrapper span |
| `nativeDigits: true` | Render in the locale native numbering system |
| `numberingSystem: 'latn' \| 'arab' \| 'deva' \| …` | Override the `Intl` numbering system |
| `style: 'currency' \| 'percent' \| 'unit' \| …` | Standard `Intl` options |

Any `Intl.NumberFormatOptions` or `Intl.DateTimeFormatOptions` key is forwarded to `Intl.*Format` so things like `maximumFractionDigits`, `dateStyle: 'long'`, and `timeZoneName: 'short'` work without explicit support here.

```njk
{{ i18n.formatDate('2026-06-04', { dateStyle: 'long' }) }}
{{ i18n.formatNumber(1200, { maximumFractionDigits: 0 }) }}
{{ i18n.formatTime(page.published_at, 'medium') }}
```

### Grammatical gender (Arabic cardinals)

Arabic cardinal numerals change form based on the gender of the noun they modify. The `formatCardinal()` formatter accepts an optional `gender` field for Arabic locales; it is ignored for `id`, `ja`, `zh`, and the `Intl` fallback. Default is `'masculine'`.

```njk
{{ i18n.formatCardinal(2) }}
{# → "اِثنان" (masculine, default) #}

{{ i18n.formatCardinal(2, { gender: 'feminine' }) }}
{# → "اِثنتان" #}

{{ i18n.formatCardinal(99, { gender: 'feminine' }) }}
{# → "تِسع وتِسْعون" #}
```

The Arabic strategy covers `0`–`999,999,999,999` (units, teens, tens, hundreds, thousands, millions). Larger numbers fall back to the raw digit string. To extend the strategy (or add support for `Intl.PluralRules` integration with `formatCurrency`), edit `src/scripts/lib/i18n/formatters.ts`.

### Pricing API

Pricing plans use a regional pricing object. `pricing.base` is the USD fallback. Add a per-locale entry when local pricing exists.

```json5
{
  pricing: {
    base: 29,
    "id-ID": 500000,
    "ja-JP": 4500,
  },
}
```

| API | Notes |
| --- | --- |
| `localPrice(plan)` | Returns the raw numeric value for the active locale (or `base`). Emits `data-local-price` for runtime re-hydration |
| `localPriceCurrency(plan)` | Returns the currency code for the active locale's price (falls back to `LOCALES[0].currency` = `IDR`). Emits `data-local-price` for runtime re-hydration |
| `formatLocalPrice(plan, options?)` | `formatCurrency(localPrice, localPriceCurrency)` |
| `convertCurrency(value, targetCurrency, options?)` | Convert from the current locale's currency to the target currency using `EXCHANGE_RATES` |
| `convertLocalPrice(plan, targetCurrency, options?)` | Convert the locale price to the target currency |
| `formatLocalPriceDiscounted(plan, multiplier, targetCurrency?, options?)` | Convert and apply a discount multiplier (`targetCurrency` is optional) |

The runtime mirror of `formatLocalPrice()` and `convertLocalPrice()` is `data-local-price` (with optional `data-discount` and `data-target-currency`). It reads the pricing payload directly from the attribute, applies the active locale, and falls back to `pricing.base` (in `USD`).

```njk
<span>{{ i18n.formatLocalPrice(plan) }}</span>
<span>{{ i18n.formatLocalPriceDiscounted(plan, 0.8) }}</span>
```

```html
<span data-local-price='{"base":29,"id-ID":500000}'></span>
```

Exchange rates are pulled from Frankfurter and committed to `generated/exchange-rates.ts`:

```bash
bun ./tools/fetch-exchange-rates.ts    # uses the cache if it is < 24h old
bun ./tools/fetch-exchange-rates.ts -- --force  # bypasses the cache
```

## Runtime

### Bootstrap

1. The Nunjucks build renders the page in the default locale and injects an inline `<script>` in `<head>`. The script reads the saved locale from `localStorage`, the path (`/{lang}/...`), or the default, and writes `window.__PAGE_ID__`, `window.__SAVED_LOCALE__`, `window.__SERVER_LOCALE__`, and `window.__USED_COMPONENTS__`. It also sets `<html lang>`, `<html dir>`, `data-script`, and toggles `.is-rtl` to avoid a flash.
2. `src/scripts/main.ts` loads `registerI18nStore` and `Alpine` in parallel, registers the Alpine i18n store, then calls `Alpine.start()`.
3. After `Alpine.start()`, `main.ts` imports and calls `initIntl(locale)` which initializes i18next, loads locale resources from `${BASE_PATH}assets/i18n/{pageID}/{locale}.json` (where `BASE_PATH` comes from `import.meta.env.BASE_PATH`). For the default locale (`id-ID`), it only updates store labels and returns early — the SSR HTML is already in the correct language. For non-default locales, it schedules `translatePage()` and `updateFormattedElements()` via the microtask queue (`scheduleTask()`). Errors during initialization are logged in dev mode.
4. `preloadActiveFont` runs as a dynamic `import()` to load the active locale's font CSS. `setupFontStackCSS`, `loadLanguageFonts`, and `watchScriptAndLoadFont` are deferred via `requestIdleCallback` (2-second timeout) to avoid blocking the initial paint.

### Window globals

Declared in `src/global.d.ts`:

| Global | Type | Purpose |
| --- | --- | --- |
| `window.__PAGE_ID__` | `keyof I18nPages` | Current page id (drives the page namespace) |
| `window.__USED_COMPONENTS__` | `(keyof I18nComponents)[]` | Components the page imports, used to namespace component locales |
| `window.__SAVED_LOCALE__` | `LocaleCode` | Locale to activate on boot (from localStorage or path) |
| `window.__SERVER_LOCALE__` | `LocaleCode` | Default locale from server config |

### i18next configuration

`initIntl()` receives an optional `localeOverride` (from `__SAVED_LOCALE__` or `__SERVER_LOCALE__`). When `localeOverride` is set, it calls `i18next.init({ … })` directly with `lng` and `detection: undefined`. The `LanguageDetector` plugin is only used as a fallback when no override is provided (which does not happen in normal bootstrap).

```ts
{
  resources,                                  // includes component data via ...compData
  supportedLngs: LOCALE_CODES,
  lng: savedLocale,
  ns: ['common', pageID],
  defaultNS: 'common',
  detection: localeOverride ? undefined : {
    order: ['localStorage', 'navigator'],
    caches: ['localStorage'],
    lookupLocalStorage: 'i18nextLocale',
  },
  interpolation: { escapeValue: false },
  saveMissing: false,
  parseMissingKeyHandler: (key) => `[${key}]`,
}
```

### Data attributes processed at runtime

`translatePage()` and `updateFormattedElements()` (in `src/scripts/lib/i18n/runtime.ts`) walk the DOM for the following attributes. The `attribute` column lists the exact attribute name produced by the corresponding template helper. Both functions are executed via `scheduleTask()` (microtask queue from `src/scripts/lib/utils/microtask-queue.ts`) for batched, non-blocking DOM updates. `data-i18n` and formatter attributes use `textContent`; `data-i18n-html` uses `innerHTML`.

| Attribute | Refreshed on locale change | Set by |
| --- | --- | --- |
| `data-i18n` | yes | `i18n.t()` |
| `data-i18n-html` | yes | Manual |
| `data-i18n-plural` + `data-i18n-count` | yes | `i18n.plural()` — stores base key (no suffix); runtime resolves plural suffix via `i18next.t()` |
| `data-i18n-attr` | yes | `i18n.attr()` (format: `attrName:translationKey`) |
| `data-i18n-vars` | (consumed by translation) | `i18n.t()` and `i18n.plural()` |
| `data-use-native` / `data-force-native` / `data-force-universal` | (consumed) | `i18n.t()` |
| `data-format-number` | yes | `formatNumber()` |
| `data-format-percent` | yes | `formatPercent()` |
| `data-format-duration` | yes | `formatDuration()` |
| `data-format-ordinal` | yes | `formatOrdinal()` |
| `data-format-cardinal` | yes | `formatCardinal()` |
| `data-format-scientific` | yes | `formatScientific()` |
| `data-format-abbreviated` | yes | `formatAbbreviated()` |
| `data-format-date` | yes | `formatDate()` |
| `data-format-date-preset` + `data-date-preset` | yes | Manual preset application |
| `data-format-datetime` | yes | `formatDateTime()` |
| `data-format-time` + `data-time-preset` | yes | `formatTime()` |
| `data-format-currency` + `data-target-currency` | yes | `formatCurrency()` |
| `data-convert-currency` + `data-target-currency` | yes | `convertCurrency()` |
| `data-convert-to-locale` | yes | Manual conversion to the active locale's currency |
| `data-local-price` + `data-discount?` + `data-target-currency?` | yes | `formatLocalPrice()`, `convertLocalPrice()`, discount helpers |
| `data-format-unit` + `data-unit` | yes | `formatUnit()` |
| `data-format-bytes` + `data-bytes-decimals` | yes | `formatBytes()` |
| `data-relative-time` + `data-unit` + `data-numeric` | yes | `formatRelativeTime()` |
| `data-format-list` + `data-items` | yes | `formatList()` |
| `data-pluralized` + `data-count` + `data-inclusive` | yes | `pluralize()` |
| `data-singularized` | yes | `singularize()` |
| `data-native-digits` + `data-force` | yes | `nativeDigits()` |

`updateFormattedElements()` iterates the static `FORMATTERS` list and is called both on `initIntl()` and after every `$store.i18n.change()`. The `FORMATTERS` array lives inline in `runtime.ts`. To add a new runtime-mutable formatter, add an entry to the `FORMATTERS` array in `runtime.ts` and a matching helper to `template.ts`.

### Alpine store

Registered by `registerI18nStore()`. The store reads from `localStorage` for `current` and writes to it on `change(code)`. The `change()` method delegates to an internal `changeLanguage()` function that orchestrates the full locale switch. On change it:

1. Saves `code` to `localStorage` under `i18nextLocale` and sets `this.current`.
2. Calls `changeLanguage(code)` which:
   - If i18next is not yet initialized, calls `initIntl(code)`.
   - Otherwise, calls `ensureLocaleData(code, pageID, m)` to fetch and load the locale's JSON bundle via `i18next.addResourceBundle()` (skips if already loaded).
   - Calls `i18next.changeLanguage(code)`.
   - Calls `scheduleTask(translatePage)`, `scheduleTask(updateFormattedElements)`, and `scheduleTask(updateI18nStoreLabels)` via the microtask queue for batched DOM updates.
3. Calls `updateDocumentAttributes(code)` to update `<html lang>`, `<html dir>`, `data-script`, and toggle `.is-rtl`.
4. Calls `refreshFonts()` which dynamically imports `fonts.ts` and calls `setupFontStackCSS()` + `loadLanguageFonts()`.

```njk
<div x-data>
  <template x-for="lang in $store.i18n.languages" :key="lang.code">
    <button
      type="button"
      :aria-pressed="$store.i18n.current === lang.code"
      @click="$store.i18n.change(lang.code)"
    >
      <span x-text="lang.label"></span>
    </button>
  </template>
</div>
```

The `languages` array is derived from `LOCALES` filtered by `LOCALE_CODES`, with the human label produced by `getLocaleLabelCountry(code)` (e.g. `English (US)`, `简体中文 (CN)`).

## User Configuration

The only file a user needs to touch to customize the i18n setup is `src/configs/i18n.ts`:

```ts
// src/configs/i18n.ts
import { defineI18n, defineFont } from '../scripts/lib/i18n/i18n-config';
import type { I18nConfig } from '../scripts/lib/i18n/types';

export const i18nConfig: I18nConfig = defineI18n({
  defaultLocale: 'id-ID',
  fonts: {
    primary: defineFont({
      name: 'inter',                 // @fontsource package name
      family: 'Inter Variable',      // CSS font-family value
    }),
    // optional:
    // secondary: defineFont({ name: 'roboto', family: 'Roboto' }),
    // monospace: defineFont({ name: 'jetbrains-mono', family: 'JetBrains Mono' }),
  },
});
```

Two fields only:

| Field | Type | Purpose |
| --- | --- | --- |
| `defaultLocale` | `LocaleCode` (BCP 47) | SSR locale, fallback target, default `getLocale()` |
| `fonts.primary` | `FontConfig` | Primary font: `@fontsource` package name + CSS family |
| `fonts.secondary` | `FontConfig` (optional) | Secondary font |
| `fonts.monospace` | `FontConfig` (optional) | Monospace font |

`defineI18n` and `defineFont` validate the config at module load time. A missing field or invalid `name` throws an error immediately, not at first render. TypeScript also enforces the shape — invalid font names are rejected at compile time.

The remaining config — `LOCALES`, `LOCALE_CODES`, the 87-locale `data.ts` array — is engine data, not user config. Add locales there if you need more than the bundled 87.

## Locale Configuration

All locale behavior is defined in the `src/scripts/lib/i18n/` package. The package combines pure data/formatters with the runtime engine (i18next init, DOM updates, Alpine store, Nunjucks template) — there is no per-locale flag in the templates. The locale data lives in `src/scripts/lib/i18n/data.ts` (the `LOCALES` array). Every locale carries the fields below:

```ts
{
  code: 'id-ID',
  language: 'id',
  region: 'ID',
  flag: 'ID',
  dir: 'ltr',
  currency: 'IDR',
  numberingSystem: 'latn',
  nativeNumberingSystem: 'latn',
  nativeDigits: false,
  writingSystem: 'latin',
  timezone: 'Asia/Jakarta',
  timezoneOffset: 7,
  calendar: 'gregory',
  dateFormat: 'short',
  timeFormat: 'short',
  firstDayOfWeek: 1,
}
```

(Plural rules are no longer a per-locale field — `getPluralSuffix` uses the browser's `Intl.PluralRules` to infer the category from the active locale.)

For Chinese script variants, include `script`:

```ts
{
  code: 'zh-Hans-CN',
  language: 'zh',
  script: 'Hans',  // required for script variants
  region: 'CN',
  // … other fields
}
```

### Key fields

| Field | Purpose | Consumers |
| --- | --- | --- |
| `code` | BCP 47 tag; the only stable identifier | Everywhere |
| `language` | `LanguageCode` — used for fallback, digit map, ordinal/cardinal strategies | `getLanguageSubtag`, formatters |
| `script` | `ScriptCode` — required for Chinese script variants | Labeling, fallback |
| `region` | `RegionCode` | Display label, fallback target |
| `flag` | `FlagCode` — emoji or short code | `$store.i18n.languages[].flag` |
| `dir` | `'ltr'` \| `'rtl'` | `data-script` neighbor, `<html dir>` |
| `currency` | `CurrencyCode` | `getCurrency()`, default formatter currency |
| `numberingSystem` | `NumberingSystemCode` used for `Intl` by default | `Intl.NumberFormat` default |
| `nativeNumberingSystem` | `NumberingSystemCode` rendered when `nativeDigits: true` | `toNativeDigits()` |
| `nativeDigits` | Whether to render native digits by default | `toNativeDigits()` (no force) |
| `writingSystem` | `WritingSystemCode` — drives `<html data-script>` and font selection | `template.ts`, `main.ts` |
| `timezone` | IANA timezone | `Intl.DateTimeFormat` |
| `timezoneOffset` | Hours from UTC (decimal for half-hour zones) | Available for client logic |
| `calendar` | `CalendarCode` | `Intl.DateTimeFormat` |
| `dateFormat` / `timeFormat` | Default presets for date and time | Available for client logic |
| `firstDayOfWeek` | `0`–`6` (Sunday = 0) | Calendar widgets |

### Derived constants

| Constant | Source | Use |
| --- | --- | --- |
| `defaultLocale` (in `i18nConfig`) | `src/configs/i18n.ts` | SSR locale, fallback target |
| `LOCALE_STORAGE_KEY` | `'i18nextLocale'` (`src/scripts/lib/i18n/index.ts`) | `localStorage` key for the saved locale |
| `LOCALE_CODES` | `LOCALES.map(l => l.code)` | `supportedLngs`, sync targets |
| `LANGUAGE_CODE` / `SCRIPT_CODE` / `REGION_CODE` / `FLAG_CODE` | Same pattern as `LOCALE_CODES` | Type-safe code constants |
| `CURRENCY_CODE` / `TIMEZONE_CODE` / `CALENDAR_CODE` / `DIRECTION_CODE` | Same pattern as `LOCALE_CODES` | Type-safe code constants |
| `NUMBERING_SYSTEM_CODE` / `WRITING_SYSTEM` | Same pattern | Type-safe code constants |

The default currency anchor for `EXCHANGE_RATES` is hardcoded to `CURRENCY_CODE.USD` in `tools/fetch-exchange-rates.ts` (the Frankfurter API base).

### Fallback chain

`getFallbackChain(locale)` in `src/scripts/lib/i18n/helpers.ts` is called from both the build (`template.ts`) and the runtime (`runtime.ts` → `fallbackLng`). The chain is:

1. If the locale is in `LOCALE_CODES`, return `[]` (no fallback needed).
2. If the language subtag matches a known locale, return `[matched, 'id-ID']`.
3. Otherwise, return `['id-ID']`.

All 87 configured locales are in `LOCALE_CODES`, so explicit fallback mapping is no longer needed — regional variants like `zh-Hans-SG` and `en-ZA` are fully configured with their own entries.

### Direction and font behavior

`data-script` on `<html>` is derived from `writingSystem` in both `template.ts` (initial render) and `store.ts` (post-change). The writing system's `defaultFont` in `writing-systems.ts` drives font selection — **changing the active locale re-runs `setupFontStackCSS()` to update `--font-primary`**, which in turn loads the appropriate `@fontsource(-variable)?/...` stylesheet.

Layout that must work in both LTR and RTL must use logical CSS properties. The RTL writing systems are: `arabic`, `hebrew`, `nko`, and `adlam`.

```css
.card {
  padding-inline: 1rem;
  margin-inline-start: 2rem;
  border-inline-end: 1px solid currentColor;
}
```

```css
/* Avoid */
.card {
  margin-left: 2rem;
  border-right: 1px solid currentColor;
}
```

## Translation Coverage

The project ships with 87 fully translated locales. The default locale `id-ID` is the source of truth — every other locale must mirror its key structure. `bun ./tools/check-locale-parity.ts` enforces this by diffing every locale against `id-ID` and exiting non-zero on any missing or extra keys. Parity enforcement is also integrated into `generate-i18n.ts` — mismatches are errors (not warnings) that fail the build with `process.exit(1)`.

### Status

| Locale | Language | Status | Plural rules | Notes |
| --- | --- | --- | --- | --- |
| `id-ID` | Indonesian | Source | other | Default locale, source of truth |
| `en-US` | English (US) | Translated | one/other | |
| `en-GB` | English (UK) | Translated | one/other | +37 regional keys |
| `en-CA` | English (Canada) | Translated | one/other | +37 regional keys |
| `en-AU` | English (Australia) | Translated | one/other | |
| `en-IN` | English (India) | Translated | one/other | +37 regional keys |
| `en-NZ` | English (New Zealand) | Translated | one/other | |
| `en-ZA` | English (South Africa) | Translated | one/other | |
| `ja-JP` | Japanese | Translated | other | |
| `zh-Hans-CN` | Chinese (Simplified, China) | Translated | other | |
| `zh-Hans-SG` | Chinese (Simplified, Singapore) | Translated | other | |
| `zh-Hans-MY` | Chinese (Simplified, Malaysia) | Translated | other | |
| `zh-Hant-TW` | Chinese (Traditional, Taiwan) | Translated | other | |
| `zh-Hant-HK` | Chinese (Traditional, Hong Kong) | Translated | other | |
| `zh-Hant-MO` | Chinese (Traditional, Macau) | Translated | other | |
| `ar-SA` | Arabic (Saudi Arabia) | Translated | zero/one/two/few/many/other | Native digits |
| `ar-AE` | Arabic (UAE) | Translated | zero/one/two/few/many/other | Native digits |
| `ar-EG` | Arabic (Egypt) | Translated | zero/one/two/few/many/other | Native digits |
| `ar-MA` | Arabic (Morocco) | Translated | zero/one/two/few/many/other | Native digits |
| `ar-TN` | Arabic (Tunisia) | Translated | zero/one/two/few/many/other | Native digits |
| `es-ES` | Spanish (Spain) | Translated | one/other | |
| `es-MX` | Spanish (Mexico) | Translated | one/other | |
| `es-AR` | Spanish (Argentina) | Translated | one/other | |
| `es-CO` | Spanish (Colombia) | Translated | one/other | |
| `es-PE` | Spanish (Peru) | Translated | one/other | |
| `pt-BR` | Portuguese (Brazil) | Translated | one/other | |
| `pt-PT` | Portuguese (Portugal) | Translated | one/other | |
| `pt-AO` | Portuguese (Angola) | Translated | one/other | |
| `pt-MZ` | Portuguese (Mozambique) | Translated | one/other | |
| `hi-IN` | Hindi (India) | Translated | one/other | |
| `hi-NP` | Hindi (Nepal) | Translated | one/other | |
| `ko-KR` | Korean (South Korea) | Translated | other | |
| `ko-KP` | Korean (North Korea) | Translated | other | |
| `fr-FR` | French (France) | Translated | one/many | |
| `fr-CA` | French (Canada) | Translated | one/many | |
| `fr-BE` | French (Belgium) | Translated | one/many | |
| `fr-CH` | French (Switzerland) | Translated | one/many | |
| `de-DE` | German (Germany) | Translated | one/other | |
| `de-AT` | German (Austria) | Translated | one/other | |
| `de-CH` | German (Switzerland) | Translated | one/other | |
| `ru-RU` | Russian (Russia) | Translated | one/few/many/other | |
| `th-TH` | Thai (Thailand) | Translated | other | |
| `nl-NL` | Dutch (Netherlands) | Translated | one/other | Added 2026-06 |
| `ca-ES` | Catalan (Spain) | Translated | one/other | Added 2026-06 |
| `it-IT` | Italian (Italy) | Translated | one/other | Added 2026-06 |
| `no-NO` | Norwegian Bokmål (Norway) | Translated | one/other | Added 2026-06 |
| `fi-FI` | Finnish (Finland) | Translated | one/other | Added 2026-06 |
| `sv-SE` | Swedish (Sweden) | Translated | one/other | Added 2026-06 |
| `da-DK` | Danish (Denmark) | Translated | one/other | Added 2026-06 |
| `cs-CZ` | Czech (Czechia) | Translated | one/few/many/other | Added 2026-06 |
| `hu-HU` | Hungarian (Hungary) | Translated | one/other | Added 2026-06 |
| `ro-RO` | Romanian (Romania) | Translated | one/few/other | Added 2026-06 |
| `pl-PL` | Polish (Poland) | Translated | one/few/many/other | Added 2026-06 |
| `el-GR` | Greek (Greece) | Translated | one/other | Added 2026-06 |
| `tr-TR` | Turkish | Translated | one/other | Added 2026-06 |
| `vi-VN` | Vietnamese | Translated | one/other | Added 2026-06 |
| `uk-UA` | Ukrainian | Translated | one/few/many/other | Added 2026-06 |
| `he-IL` | Hebrew | Translated | one/two/many/other | RTL, Added 2026-06 |
| `fa-IR` | Persian | Translated | one/other | RTL, native digits, Added 2026-06 |
| `bn-BD` | Bengali (Bangladesh) | Translated | one/other | Native digits, Added 2026-06 |
| `bn-IN` | Bengali (India) | Translated | one/other | Native digits, Added 2026-06 |
| `pa-IN` | Panjabi (Gurmukhi, India) | Translated | one/other | Native digits, Added 2026-06 |
| `pa-PK` | Panjabi (Shahmukhi, Pakistan) | Translated | one/other | RTL, native digits, Added 2026-06 |
| `gu-IN` | Gujarati | Translated | one/other | Native digits, Added 2026-06 |
| `mr-IN` | Marathi | Translated | one/other | Native digits, Added 2026-06 |
| `ta-IN` | Tamil (India) | Translated | one/other | Native digits, Added 2026-06 |
| `ta-LK` | Tamil (Sri Lanka) | Translated | one/other | Native digits, Added 2026-06 |
| `te-IN` | Telugu | Translated | one/other | Native digits, Added 2026-06 |
| `kn-IN` | Kannada | Translated | one/other | Native digits, Added 2026-06 |
| `ml-IN` | Malayalam | Translated | one/other | Native digits, Added 2026-06 |
| `kk-KZ` | Kazakh | Translated | one/other | Added 2026-06 |
| `uz-UZ` | Uzbek (Latin) | Translated | one/other | Added 2026-06 |
| `uz-AF` | Uzbek (Arabic, Afghanistan) | Translated | one/other | RTL, native digits, Added 2026-06 |
| `ky-KG` | Kyrgyz | Translated | one/other | Added 2026-06 |
| `tg-TJ` | Tajik | Translated | one/other | Added 2026-06 |
| `mn-MN` | Mongolian | Translated | one/other | Added 2026-06 |
| `ka-GE` | Georgian | Translated | one/other | Added 2026-06 |
| `hy-AM` | Armenian | Translated | one/other | Added 2026-06 |
| `az-AZ` | Azerbaijani (Latin) | Translated | one/other | Added 2026-06 |
| `sr-RS` | Serbian (Cyrillic) | Translated | one/few/many/other | Added 2026-06 |
| `hr-HR` | Croatian | Translated | one/other | Added 2026-06 |
| `sl-SI` | Slovenian | Translated | one/two/few/other | Added 2026-06 |
| `sk-SK` | Slovak | Translated | one/few/many/other | Added 2026-06 |
| `bg-BG` | Bulgarian | Translated | one/other | Added 2026-06 |
| `lt-LT` | Lithuanian | Translated | one/few/many/other | Added 2026-06 |
| `lv-LV` | Latvian | Translated | zero/one/other | Added 2026-06 |
| `ee-EE` | Estonian | Translated | one/other | Added 2026-06 |

Total: **87** locales, **86** translated (all non-default), **0** untranslated.

### Translation strategy

The **45 locales added in June 2026** (12 European Latin + 33 across Turkic, Central Asian, South Asian Indic, Southeast Asian, Middle Eastern/Caucasus, Slavic/Baltic/Balkan) were translated per-locale manually with the following conventions:

- **Per-locale plural rules** (from CLDR): each locale uses its native plural categories instead of copying Indonesian's 6-variant template. For example, Dutch uses `one`/`other`; Czech uses `one`/`few`/`many`; Polish uses `one`/`few`/`many`; Arabic uses all six categories. Plural keys absent from the target locale are not generated.
- **Native script and numbering**: European Latin locales use `LATN` digits; South Asian Indic locales use their script's native numbering system (`BENG`, `GUJR`, `GURU`, `DEVA`, `TAMLDEC`, `TELU`, `KNDA`, `MLYM`) with `nativeDigits: true`. Cyrillic-script locales (`kk-KZ`, `ky-KG`, `tg-TJ`, `mn-MN`, `uk-UA`, `sr-RS`, `bg-BG`) use `CYRL`. RTL locales (`he-IL`, `fa-IR`, `pa-PK`, `uz-AF`) use `ARAB` or `HEBR` with appropriate native digit settings.
- **Locale-specific currency**: each locale is bound to its actual currency. Prices and currency formatting reflect the local currency.
- **Localized contact details**: phone numbers and addresses reflect the locale's primary city (e.g. `+90 312 123 4567` / "Ankara, Turkiye" for `tr-TR`).
- **Brand and technical terms kept in English**: "Technomira Tanzania", "Starter", "SOC 2", "GDPR", "Web Engine", "API", "SSO" remain in English across all locales.
- **HTML in values preserved**: tags like `<span class='text-violet-400'>...</span>` are kept intact for color highlighting in titles.
- **Placeholders preserved**: `{{count}}`, `{{year}}` are left as-is for runtime interpolation.

### Per-locale plural forms

The `Intl.PluralRules` category that i18next appends to plural key suffixes is automatically inferred from the active locale — no per-locale config required. Categories used by the bundled locales:

| Category | Locales |
| --- | --- |
| `other` only | `ja-JP`, `zh-*`, `ko-*`, `th-TH`, `vi-VN` |
| `one` + `other` | `en-*`, `id-ID`, `de-*`, `es-*`, `pt-*`, `hi-*`, `nl-NL`, `ca-ES`, `it-IT`, `no-NO`, `fi-FI`, `sv-SE`, `da-DK`, `hu-HU`, `el-GR`, `tr-TR`, `fa-IR`, `bn-*`, `pa-*`, `gu-IN`, `mr-IN`, `ta-*`, `te-IN`, `kn-IN`, `ml-IN`, `kk-KZ`, `uz-*`, `ky-KG`, `tg-TJ`, `mn-MN`, `ka-GE`, `hy-AM`, `az-AZ`, `hr-HR`, `bg-BG`, `lv-LV`, `ee-EE` |
| `one` + `many` | `fr-*` |
| `one` + `two` + `many` + `other` | `he-IL` |
| `one` + `few` + `many` + `other` | `cs-CZ`, `pl-PL`, `sk-SK`, `lt-LT`, `ru-RU`, `uk-UA`, `sr-RS` |
| `one` + `few` + `other` | `ro-RO` |
| `one` + `two` + `few` + `other` | `sl-SI` |
| `zero` + `one` + `two` + `few` + `many` + `other` | `ar-*` |

See [Plural keys](#plural-keys) for the suffix mapping.

### Algorithmic numbering systems

`numbering-systems.ts` defines 33 systems, of which 12 are **algorithmic** (spellout rules) and 21 are **numeric** (digit-array based). The algorithmic systems are: `armn` (Armenian), `cyrl` (Cyrillic), `ethi` (Ethiopic), `geor` (Georgian), `grek` (Greek), `hebr` (Hebrew), `roman` (Roman), `taml` (Tamil traditional), plus the four CJK spellout systems (`jpan`/`hans`/`hant`/`kore`). Of the 12, five are bound to locales (`jpan`/`hans`/`hant`/`kore`/`cyrl`) and the rest are wired at the system level but not yet bound.

| Code | Writing system | Font loader | Digit array | Status |
| --- | --- | --- | --- | --- |
| `armn` | Armenian | `@fontsource-variable/noto-sans-armenian` | 10 glyphs (Ա–Ժ) | Wired |
| `cyrl` | Cyrillic (pre-1918 numerals) | Primary font (Cyrillic subset) | 10 glyphs (оа–ѳ) | Wired |
| `ethi` | Ethiopic | `@fontsource-variable/noto-sans-ethiopic` | 10 glyphs (፩–፲) | Wired |
| `geor` | Georgian (Mkhedruli) | `@fontsource-variable/noto-sans-georgian` | 10 glyphs (ა–კ) | Wired (added 2026-06-07) |
| `grek` | Greek | Primary font (Greek + Greek-ext subsets) | 10 glyphs (Α–Ι, Ϛ) | Wired |
| `hebr` | Hebrew | `@fontsource-variable/noto-sans-hebrew` | 10 glyphs (א–י) | Wired |
| `roman` | Latin (Roman) | Primary font (Latin) | `null` (sign-value, 7 symbols, no 0) | Wired (added 2026-06-07) |
| `taml` | Tamil (traditional) | `@fontsource-variable/noto-sans-tamil` | 10 glyphs (௦–௯) | Wired (added 2026-06-07) |

> Algorithmic systems can still carry a `digits` array — they act as a **decimal fallback** for environments where `Intl.NumberFormat` doesn't implement the spellout (e.g. Bun, limited ICU builds). Browsers with full ICU data render the script-native spellout, and the digit array is used by `toNativeDigits()` for any decimal value the runtime can't spell out. `roman` is the only exception: its 7-symbol sign-value set has no decimal form, so its `digits` stays `null`.

### Additional numeric systems

Four more `numeric`-type systems were added in June 2026 to expand digit glyph coverage beyond the original 18:

| Code | Writing system | Region | Font loader | Status |
| --- | --- | --- | --- | --- |
| `orya` | Odia | India (Odisha) | `@fontsource/noto-sans-oriya` | Wired |
| `tibt` | Tibetan | Tibet / Bhutan | `@fontsource-variable/noto-serif-tibetan` | Wired |
| `nkoo` | N'Ko | West Africa (Mali, Guinea) | `@fontsource/noto-sans-nko` | Wired |
| `adlm` | Adlam | Guinea | `@fontsource-variable/noto-sans-adlam` | Wired |

> Tibetan is only available in the **serif** style on `@fontsource` (the sans-serif variant isn't published). N'Ko and Odia are non-variable packages (specific weights) — the variable version isn't published on npm for those scripts.

### Demonstration

The i18n test page (`/i18n-test`) includes a **Writing Systems Demo** section that renders the same number (`2024`) through each of the algorithmic and additional numeric systems to confirm the loader, font family (from `writing-systems.ts` `defaultFont`), and runtime chain are connected. The runtime also accepts a per-element `data-numbering-system` attribute on `data-format-number`, `data-format-percent`, `data-format-currency`, `data-format-scientific`, and `data-convert-currency` elements.

> **Note:** Some runtime environments (e.g. older or stripped-down ICU builds) don't implement the algorithmic spellout for `grek`/`hebr`/`armn`/`ethi`/`taml`/`geor`/`roman` and fall back to Latin digits. The font, loader, and metadata stay valid — once a locale binds to one of these systems, browsers with full ICU data will render the script correctly.

### Coverage report

Run `bun ./tools/check-locale-parity.ts` to regenerate the table above. The script reports `Missing` and `Extra` key counts per locale:

```bash
$ bun ./tools/check-locale-parity.ts
LOCALE PARITY CHECK REPORT
Summary by Locale:
| Locale       | Missing | Extra | Status |
| id-ID        |       0 |     0 | OK     |
| nl-NL        |       0 |     0 | OK     |
...
```

The 111 pre-existing extra-key issues in `en-GB`, `en-CA`, `en-IN` (37 each) are known and unrelated to the newly-added locales — they reflect regional-specific keys that the bundled English variants extend with.

### Adding a translation to an existing untranslated locale

If a locale exists in `LOCALES` but its files are still copies of `id-ID`:

1. Open the file under `src/locales/{locale}/`.
2. Translate the values, keeping the key structure and `{{placeholder}}` syntax intact.
3. Remove plural variants that do not exist for the target locale's CLDR plural rules (e.g. drop `_zero`/`_two`/`_few`/`_many` for Dutch).
4. Run `bun ./tools/check-locale-parity.ts` to confirm.
5. Run `bun run build` to confirm SSR + runtime.

## Type Safety

`bun ./tools/generate-i18n.ts` walks `src/locales/id-ID` and writes `generated/i18n.d.ts`. The file contains:

- `I18nTranslationKeys` — discriminated union of all `'common:key'`, `'{page}:key'`, and `'components.{name}:key'` strings.
- `I18nPages` — interface indexed by page id, with each value matching that page's JSON shape.
- `I18nComponents` — interface indexed by component name, with each value matching that component's JSON shape.

`src/scripts/lib/i18n/runtime.ts` exports a typed `t(key: I18nTranslationKeys, vars?)` and `src/global.d.ts` references `I18nPages` and `I18nComponents` for the window globals. This is the only way TypeScript can validate keys in client code.

> The type generator runs as part of `bun run build`. Run manually with `bun ./tools/generate-i18n.ts`. Parity checking is integrated into `generate-i18n.ts` — locale key mismatches are treated as errors that fail the build (`process.exit(1)`). For a standalone parity report, use `bun ./tools/check-locale-parity.ts`.

## Tooling and Workflows

### Commands

| Command | Description |
| --- | --- |
| `bun run dev` | Sync root page, clean cache, fetch rates, generate i18n types, watch locale JSON files, run Rsbuild dev server |
| `bun run build` | Sync root page, clean cache, fetch rates, generate i18n types, generate sitemap, manifest, robots, production build |
| `bun run preview` | Tunnel orchestrator — ngrok or cloudflared, build, and serve |
| `bun run serve` | Serve the production build locally |
| `bun ./tools/generate-sitemap.ts` | Generate `public/sitemap.xml` from page directories |
| `bun ./tools/generate-page.ts <name>` | Scaffold a new page folder with 87 locale files |
| `bun ./tools/sync-locales.ts` | Create missing locale directories from default |
| `bun run clean:cache` | Remove `node_modules/.cache`, `.cache`, `dist` |
| `bun run test` | Run tests via `rstest` |
| `bun run cli` | Interactive menu for all tools |

The following tools are available as direct `bun ./tools/<name>.ts` commands:

| Tool | Description |
| --- | --- |
| `tools/check-locale-parity.ts` | Diff keys across all locales; exits non-zero on issues (also enforced by `generate-i18n.ts` as a build error). |
| `tools/fetch-exchange-rates.ts` | Refresh exchange rates. Use `--force` or `-f` to bypass the 24h cache. |

All tools use shared modules from `tools/shared/`: `logger.ts` for centralized output (`log.*()` — 13 methods: info, error, warn, success, cancelled, header, toolStarted, toolFailed, toolSpawnFailed, serverReady, serverPortError, serverError, distNotFound) and `signal-handler.ts` for graceful shutdown and error wrapping.

### Add page copy

1. Edit `src/locales/id-ID/{page}.json` (the file is created by `bun ./tools/generate-page.ts`).
2. Mirror the key structure in every other locale under `src/locales/`.
3. Use `{{ i18n.t('{page}:section.key') }}` in the template.
4. Run `bun ./tools/check-locale-parity.ts`.

### Add reusable component copy

1. Create or edit `src/locales/id-ID/components.{name}.json` (flat file, dot-notation namespace).
2. Include the component in the page template (`{% include 'components/{name}.njk' %}`). `getUsedComponents()` picks it up automatically.
3. Use `{{ i18n.t('components.{name}:key') }}`.

The flat file name (`components.{name}.json`) determines the i18next namespace (`components.{name}`). `i18n-ally` discovers namespaces from the same file naming convention, so the type system (`components.{name}:heading`), runtime, and editor extension all agree on the same namespace.

### Add a new locale

1. Add the entry to `LOCALES` in `src/scripts/lib/i18n/data.ts`.
2. Add the language, region, currency, flag, timezone, and (if new) writing system + numbering system entries to the matching per-concern file under `src/scripts/lib/i18n/`.
3. Run `bun ./tools/sync-locales.ts` to copy the default structure.
4. Translate the new JSON files. Apply per-locale plural rules and keep brand/technical terms in English. See [Translation Coverage](#translation-coverage) for the conventions used across the 33 locales added in June 2026.
5. Run `bun ./tools/check-locale-parity.ts` to verify key parity.
6. Run `bun run build` to confirm the locale renders end-to-end.

## Best Practices

### Use `i18n.*` for user-facing copy

```njk
<h1>{{ i18n.t('home:hero.title') }}</h1>
```

### Use formatters instead of manual formatting

```njk
<span>{{ i18n.formatCurrency(plan.price, 'IDR') }}</span>
<time>{{ i18n.formatDate(page.published_at, { dateStyle: 'medium' }) }}</time>
```

### Use `raw` only when wrappers are not allowed

```njk
<meta name="description" content="{{ i18n.t('home:seo.description', {}, { raw: true }) }}">
```

### Use `attr()` for simple attributes

```njk
<input {{ i18n.attr('contact:form.email_placeholder', 'placeholder') }}>
```

### Use logical CSS for RTL-safe layouts

```njk
<div class="mbs-4 mie-2 pie-3 border-is"></div>
```

### Avoid hardcoded user-facing copy

```njk
<!-- Avoid -->
<h1>Welcome</h1>
```

### Avoid manual currency or date formatting

```njk
<!-- Avoid -->
<span>Rp {{ price }}</span>
```

## Troubleshooting

### Quick diagnosis flowchart

```
Page looks wrong on a specific locale
            │
            ▼
  Is the right text showing?  ──No──►  check:parity, missing keys, fallback chain
            │ Yes
            ▼
  Is the right font showing?  ──No──►  See "Font issues" below
            │ Yes
            ▼
  Is the right number format?  ──No──►  Check i18n.format*() usage
            │ Yes
            ▼
  Is the right direction (RTL)?  ──No──►  Check logical CSS properties
```

### Common issues

| Symptom | Likely cause | Fix |
| --- | --- | --- |
| `[home:hero.title]` is displayed | Missing key in the active resource | Add the key to the relevant locale and run `bun ./tools/generate-i18n.ts` |
| SSR copy is correct but does not change after switching language | The output has no runtime data attribute | Use `i18n.t()` or a formatter helper |
| Attribute text does not change after switching language | `i18n.attr()` only renders the initial attribute | Use the `data-i18n-attr="attr:key"` manual pattern for runtime attribute updates |
| A number or currency does not change | The value is rendered manually | Use `i18n.formatNumber()` / `formatCurrency()` / `convertCurrency()` |
| Native digits disappear on language switch | The locale's `nativeDigits` is `false` so `toNativeDigits()` skips conversion | Switch to `i18n.t()` with a translation key, or `i18n.formatNumber(..., { nativeDigits: true })` |
| Price uses the wrong currency | The plan has no locale price and falls back to `base` | Add a locale-specific price or pass an explicit `targetCurrency` |
| RTL layout is broken | CSS uses physical direction properties | Use logical properties (`margin-inline-start`, `border-inline-end`, etc.) |
| Font does not switch with the locale | `setupFontStackCSS()` did not run | The `lib/i18n/store.ts` `change()` method dynamically imports `fonts.ts` and calls `setupFontStackCSS()` + `loadLanguageFonts()` after i18next finishes; check the dynamic import isn't failing in DevTools |
| `<html data-script>` is `latin` for an Arabic locale | `data-script` is derived from `writingSystem`, not `numberingSystem` — for `ar-*` it is `arabic` by config | Verify `writingSystem` on the locale entry; this is by design |
| Build fails with type errors | JSON was edited and types are stale | Run `bun ./tools/generate-i18n.ts` |
| `bun ./tools/check-locale-parity.ts` fails | Locale files have missing or extra keys | Run with `--verbose` and align the key structure (`bun ./tools/check-locale-parity.ts -- --verbose`) |
| A component translation is empty at runtime | The component is not included by the page | `getUsedComponents()` only loads locales for components the template imports; add the include |
| A locale button label is `id-ID` instead of `Indonesian (ID)` | `getLocaleLabelCountry()` did not match | Confirm the locale entry exists in `LOCALES` |

### Font issues

| Symptom | Likely cause | Fix |
| --- | --- | --- |
| System font renders (Yu Gothic, Hiragino, etc.) instead of Noto Sans | `--font-primary` doesn't match the `font-family` registered by `@fontsource` | Compare `writing-systems.ts` `defaultFont` with `@font-face` in `node_modules/@fontsource*/index.css`; update the config to match exactly |
| Font doesn't load on page refresh (only on language switch) | The locale's numbering system isn't in `NOTO_SANS` | Add the entry to the `NOTO_SANS` map in `lib/i18n/fonts.ts` |
| "Module not found" during build | Missing `@fontsource*` package or wrong import path | Add to `package.json`; check `variable` vs non-variable path |
| First switch to a locale shows tofu briefly | First-time download of font CSS | Add to `NOTO_SANS` in `lib/i18n/fonts.ts` for next refresh, or accept FOUT |

### Debug commands

```bash
# See which keys differ across locales
bun ./tools/check-locale-parity.ts -- --verbose

# Inspect what was generated into the inline script
bun run build
grep "window.__SAVED_LOCALE__" dist/index.html | head -c 200

# Check the rendered --font-primary for a locale
grep "font-primary" dist/index.html | head -c 300

# Verify the locale is stored correctly
# In DevTools: localStorage.getItem('i18nextLocale')
# In DevTools: document.documentElement.lang
# In DevTools: document.documentElement.style.getPropertyValue('--font-primary')
```

### Common `data-*` attribute reference

| Attribute | Example | Runtime behavior |
| --- | --- | --- |
| `data-i18n="key"` | `<span data-i18n="home:hero.title">` | Sets `textContent` to the translation |
| `data-i18n-html="key"` | `<h1 data-i18n-html="home:hero.title_html">` | Sets `innerHTML` (allows markup) |
| `data-i18n-attr="alt:key"` | `<img data-i18n-attr="alt:home:hero.alt">` | Sets the named attribute |
| `data-i18n-plural="key"` | `<span data-i18n-plural="common:items" data-i18n-count="3">` | Stores base key; `i18next.t()` resolves plural suffix per locale |
| `data-format-number="1234.56"` | `<span data-format-number="1234.56">` | Locale-formatted number |
| `data-format-currency="99" data-target-currency="USD"` | `<span data-format-currency="99" data-target-currency="USD">` | Locale-formatted currency |
| `data-convert-currency="100" data-target-currency="EUR"` | `<span data-convert-currency="100" data-target-currency="EUR">` | Converted currency |
| `data-local-price='{"USD":99,"EUR":89}'` | `<span data-local-price='{"USD":99}'>` | Locale-specific pricing |
| `data-format-date="2026-06-04"` | `<time data-format-date="2026-06-04">` | Locale-formatted date |
| `data-format-unit="100" data-unit="kilometer"` | `<span data-format-unit="100" data-unit="kilometer">` | Locale-formatted unit |
| `data-relative-time="-86400" data-unit="day"` | `<span data-relative-time="-86400" data-unit="day">` | Relative time ("1 day ago") |

## Reference

### Helper exports from `src/scripts/lib/i18n/helpers.ts`

| Export | Signature | Use |
| --- | --- | --- |
| `setLocale(locale)` | `(LocaleCode) => void` | Sets the process-locale used by `getLocale()` |
| `getLocale(locale?)` | `(LocaleCode?) => LocaleCode` | Returns the input if configured, otherwise walks the fallback chain |
| `getFallbackChain(locale)` | `(string) => LocaleCode[]` | Ordered list of fallbacks |
| `getLanguageSubtag(locale)` | `(LocaleCode) => LanguageCode` | `zh-Hans-CN` → `zh` |
| `getLanguageConfig(locale)` | `(LocaleCode) => LocaleConfig \| undefined` | Locale entry lookup with prefix match |
| `getCurrency(locale)` | `(LocaleCode) => CurrencyCode` | Returns the locale's currency, defaults to `LOCALES[0].currency` if the locale code is unknown |
| `getTimezone(locale)` | `(LocaleCode) => string` | Defaults to `'UTC'` |
| `getTimezoneOffset(locale)` | `(LocaleCode) => number` | Defaults to `0` |
| `getCalendar(locale)` | `(LocaleCode) => string` | Defaults to `'gregory'` |
| `getFirstDayOfWeek(locale)` | `(LocaleCode) => number` | Defaults to `0` |
| `getNumberingSystem(locale)` | `(LocaleCode) => string` | Defaults to `'latn'` |
| `getDefaultNativeDigits(locale)` | `(LocaleCode) => boolean` | Defaults to `false` |
| `getRegionSubtag(locale)` | `(LocaleCode) => string \| undefined` | `id-ID` → `'ID'` |
| `getDirection(locale)` | `(LocaleCode) => DirectionCode` | Defaults to `'ltr'` |
| `isRTL(locale)` | `(LocaleCode) => boolean` | Convenience over `getDirection` |
| `getPluralSuffix(n, locale?)` | `(number, LocaleCode?) => string` | Returns `'_one'`, `'_other'`, etc. |

### Helper exports from `src/scripts/lib/i18n/formatters.ts`

| Export | Signature | Use |
| --- | --- | --- |
| `toNativeDigits(text, force?)` | `(string, boolean?) => string` | Converts digits to the locale's native digits |

## Fonts

Font loading is part of the i18n system — it is driven by locale data, reacts to locale changes via `MutationObserver`, and lives at `src/scripts/lib/i18n/fonts.ts`.

### Hybrid loading strategy

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Page load (refresh)                          │
│                                                                     │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────┐  │
│  │  Inline script   │    │  Static import   │    │  Preload     │  │
│  │  (template.ts)   │    │  (fonts.ts)      │    │  (preload)   │  │
│  └────────┬─────────┘    └────────┬─────────┘    └──────┬───────┘  │
│           │                       │                       │         │
│           ▼                       ▼                       ▼         │
│   <html lang="ja-JP">      Noto Sans Latin         dynamic import()│
│   data-script="cjk"        (always loaded)         noto-sans-jp.css │
│   --font-primary set                                (high priority)│
└─────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    User changes language                            │
│                                                                     │
│  Alpine store  ──►  <html lang="ar-SA">  ──►  MutationObserver     │
│   .change()          data-script="arabic"      detects change      │
│                                                                     │
│                                      ──►  Dynamic import of        │
│                                           noto-sans-arabic CSS     │
└─────────────────────────────────────────────────────────────────────┘
```

Three layers, one file (`lib/i18n/fonts.ts`):

1. **Static** (always loaded) — `import '@fontsource-variable/inter/index.css'` at top of `fonts.ts`. Guarantees the default locale (`id-ID` / Latin) renders correctly.
2. **Preload** (active locale on refresh) — `preloadActiveFont()` dynamically imports the current locale's font CSS via `import()`. Browser fetches in parallel with HTML parsing.
3. **On-demand** (locale changes at runtime) — `watchScriptAndLoadFont()` runs `MutationObserver` on `<html lang>` and `<html data-script>`. On change, dynamically imports the new font's CSS.

### File map (font-related)

| Concern | Where |
|---------|-------|
| Font config (name, family) | `src/configs/i18n.ts` (`i18nConfig.fonts`) |
| Font builders + types | `src/scripts/lib/i18n/i18n-config.ts` + `src/scripts/lib/i18n/types.ts` |
| Per-writing-system `defaultFont` (for `--font-primary`) | `src/scripts/lib/i18n/writing-systems.ts` (`WRITING_SYSTEMS`) |
| Per-script Noto Sans CSS paths + dynamic loaders | `src/scripts/lib/i18n/fonts.ts` (`NOTO_SANS` — 26 entries) |
| Public API (called by `main.ts` and `lib/i18n/store.ts`) | `src/scripts/lib/i18n/fonts.ts` |

### Why is each CSS path written twice in `NOTO_SANS`?

Each entry in `NOTO_SANS` looks like:

```ts
jpan: {
  css: '@fontsource-variable/noto-sans-jp/index.css',
  loader: () => import('@fontsource-variable/noto-sans-jp/index.css'),
},
```

The string appears once in `css` (for the preload link) and once inside the loader's `import()` call. **The duplication is required for code splitting.**

`import(constVariable)` (using a top-level const for the path) makes Rsbuild/Rspack merge all 26 fonts into one CSS chunk. The literal string in the loader's `import()` is what lets the bundler create per-font chunks that are loaded on demand.

If you add a new entry, copy the string in both places — they must match exactly.

### Font name resolution

CSS uses a `--font-primary` variable that must match the `font-family` registered by `@fontsource`. The font family is defined per writing system in `writing-systems.ts` (`defaultFont`):

| Writing System | `--font-primary` value | Package |
|---------------|------------------------|---------|
| `latin` | `"Inter Variable, system-ui"` | `@fontsource-variable/inter` |
| `cjk` | `"Noto Sans JP/SC/TC/KR Variable"` | `@fontsource-variable/noto-sans-jp/sc/tc/kr` |
| `arabic` | `"Noto Sans Arabic Variable"` | `@fontsource-variable/noto-sans-arabic` |
| `devanagari` | `"Noto Sans Devanagari"` | `@fontsource/noto-sans-devanagari` |
| `cyrillic` | `"Inter Variable"` | `@fontsource-variable/inter` |
| `thai` | `"Noto Sans Thai Variable"` | `@fontsource-variable/noto-sans-thai` |
| `bengali` | `"Noto Sans Bengali Variable"` | `@fontsource-variable/noto-sans-bengali` |
| `tamil` | `"Noto Sans Tamil Variable"` | `@fontsource-variable/noto-sans-tamil` |
| `telugu` | `"Noto Sans Telugu Variable"` | `@fontsource-variable/noto-sans-telugu` |
| `kannada` | `"Noto Sans Kannada Variable"` | `@fontsource-variable/noto-sans-kannada` |
| `malayalam` | `"Noto Sans Malayalam Variable"` | `@fontsource-variable/noto-sans-malayalam` |
| `gujarati` | `"Noto Sans Gujarati Variable"` | `@fontsource-variable/noto-sans-gujarati` |
| `gurmukhi` | `"Noto Sans Gurmukhi Variable"` | `@fontsource-variable/noto-sans-gurmukhi` |
| `sinhala` | `"Noto Sans Sinhala Variable"` | `@fontsource-variable/noto-sans-sinhala` |
| `georgian` | `"Noto Sans Georgian Variable"` | `@fontsource-variable/noto-sans-georgian` |
| `armenian` | `"Noto Sans Armenian Variable"` | `@fontsource-variable/noto-sans-armenian` |
| `ethiopic` | `"Noto Sans Ethiopic Variable"` | `@fontsource-variable/noto-sans-ethiopic` |
| `khmer` | `"Noto Sans Khmer Variable"` | `@fontsource-variable/noto-sans-khmer` |
| `lao` | `"Noto Sans Lao Variable"` | `@fontsource-variable/noto-sans-lao` |
| `myanmar` | `"Noto Sans Myanmar Variable"` | `@fontsource-variable/noto-sans-myanmar` |
| `greek` | `"Inter Variable"` | `@fontsource-variable/inter` |
| `hebrew` | `"Noto Sans Hebrew Variable"` | `@fontsource-variable/noto-sans-hebrew` |
| `odia` | `"Noto Sans Oriya"` | `@fontsource/noto-sans-oriya` |
| `tibetan` | `"Noto Serif Tibetan Variable"` | `@fontsource-variable/noto-serif-tibetan` |
| `nko` | `"Noto Sans NKo"` | `@fontsource/noto-sans-nko` |
| `adlam` | `"Noto Sans Adlam Variable"` | `@fontsource-variable/noto-sans-adlam` |

`setupFontStackCSS()` reads the writing system's `defaultFont` and sets `--font-primary` on `<html>`.

### Adding a new writing system

Example: adding Bengali (`beng`).

1. **Add the package to `package.json`:**
   ```json
   "@fontsource-variable/noto-sans-bengali": "^5.2.10"
   ```

2. **Add the entry to `WRITING_SYSTEMS`** in `src/scripts/lib/i18n/writing-systems.ts`:
   ```ts
   {
     code: 'bengali',
     name: 'Bengali Script',
     nameId: 'Skrip Bengali',
     description: 'Bengali script used in Bengali and Assamese languages',
     languages: [],
     numberingSystems: ['beng'],
     direction: DIRECTION_CODE.LTR,
     defaultFont: 'Noto Sans Bengali Variable',
   }
   ```

3. **Add the entry to `NOTO_SANS`** in `src/scripts/lib/i18n/fonts.ts` (remember: both `css` and `loader` need the literal string):
   ```ts
   beng: {
     css: '@fontsource-variable/noto-sans-bengali/index.css',
     loader: () => import('@fontsource-variable/noto-sans-bengali/index.css'),
   },
   ```
   The `SCRIPT_FONT_LOADERS` and `SCRIPT_FONT_CSS` maps are derived from this automatically.

4. **Add at least one locale** with `nativeNumberingSystem: NUMBERING_SYSTEM_CODE.BENG` in `src/scripts/lib/i18n/data.ts`.

5. **Run `bun run build`** to verify the font bundle is created in `dist/assets/fonts/`.

### Helper exports from `src/scripts/lib/i18n/runtime.ts`

| Export | Signature | Use |
| --- | --- | --- |
| `i18next` | `typeof i18next` | The initialized i18next instance |
| `initIntl()` | `(localeOverride?: string) => Promise<void>` | Reads `__SAVED_LOCALE__` or `__SERVER_LOCALE__`, initializes i18next, runs translation and formatter refresh |
| `t(key, vars?)` | `(I18nTranslationKeys, Record<string, …>?) => string` | Type-safe runtime translation |
| `translatePage()` | `() => void` | Schedules `data-i18n*` attribute walks via microtask queue |
| `updateFormattedElements()` | `() => void` | Schedules `data-format-*` attribute walks via microtask queue |
| `updateI18nStoreLabels()` | `() => void` | Refreshes `$store.i18n.languages` from the current config |

### Reading the source

- User config (the only file you should touch for setup): `src/configs/i18n.ts`
- All i18n code (data, formatters, runtime, store, template): `src/scripts/lib/i18n/`
  - Master data: `data.ts` (87 locales, 54 languages)
  - Per-concern: `languages.ts`, `regions.ts`, `currencies.ts`, `timezones.ts`, `numbering-systems.ts`, `writing-systems.ts`, `flags.ts`, `directions.ts`, `calendars.ts`
  - Types: `types.ts` (FontConfig, I18nConfig, etc.)
  - Builders: `i18n-config.ts` (`defineI18n`, `defineFont`)
  - Engine: `helpers.ts` (getLocale, getCurrency, etc.), `formatters.ts` (Intl formatters), `runtime.ts` (i18next init, inline `FORMATTERS` array), `store.ts` (Alpine), `template.ts` (Nunjucks), `fonts.ts` (font loader — 26 NOTO_SANS entries)  - Barrel: `index.ts` (re-exports data + helpers + formatters)
- Bootstrap orchestration: `src/scripts/main.ts`
- Microtask queue: `src/scripts/lib/utils/microtask-queue.ts` (`scheduleTask`)
- Generated keys and rate table: `generated/i18n.d.ts`, `generated/exchange-rates.ts` (imported via relative paths; jiti doesn't resolve `@generated/` alias)

Last updated: 2026-06-13 (generated file stubs committed for fresh-clone typecheck, relative import paths for jiti compatibility, UI polish)

{% extends "public/main.njk" %}

{% block title %}{{ meta.title }}{% endblock %}

{% block content %}
<section class="py-20 px-6 min-h-screen">
  <div class="container mx-auto">
    <h1 class="text-4xl font-bold mb-4">{{ hero.title }}</h1>
    <p class="text-lg text-slate-600">{{ hero.description }}</p>
  </div>
</section>
{% endblock %}
